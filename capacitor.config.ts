import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.liquidglass.studio',
  appName: 'Liquid Glass Studio',
  webDir: 'dist',
};

export default config;
